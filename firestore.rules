rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true; 
      match /pending/{submissions} {
        allow write;
      }
      match /pending/{submissions} {
      allow update, delete: if request.auth.uid == resource.data.author_uid;
      }
    }
  }
}